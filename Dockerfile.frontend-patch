FROM nvcr.io/nvidia/blueprint/rag-frontend:2.4.0

# Replace compiled frontend dist with our updated build
COPY frontend/dist /app/frontend/dist

# Replace vite config so proxy rules (including /api/crawl) are picked up at runtime
COPY frontend/vite.config.ts /app/frontend/vite.config.ts
